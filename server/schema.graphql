### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Mutation {
  authenticate(email: String!, password: String!): User!
  createDraft(authorId: ID!, body: String!, postedOn: String!, title: String!): Post!
  createProduct(body: String!, collection: String!, color: String!, price: String!, size: String!, title: String!, type: String!): Product!
  createUser(email: String!, name: String!, password: String!): User!
  deleteDraft(draftId: String!): Post
  deleteProduct(id: ID!): Product!
  deleteUser(id: ID!): User!
  publishDraft(draftId: String!): Post
  updateProduct(body: String, collection: String, color: String, id: ID!, price: String, size: String, title: String, type: String): Product!
  updateUser(email: String, id: ID!, name: String, password: String): User!
}

type Post {
  authorId: ID
  body: String
  id: ID
  postedOn: String
  published: Boolean
  title: String
}

type Product {
  body: String
  collection: String
  color: String
  id: ID
  price: String
  size: String
  title: String
  type: String
}

type Query {
  drafts: [Post]
  posts: [Post]
  products: [Product]
  users: [User]
}

type User {
  email: String!
  id: ID
  name: String!
  password: String!
  posts: [Post]
  role: String
}